stages:
  - release

package_release:
  image: hacksawgaming/hacksaw-build:4.0.0-rc.2-10
  stage: release
  before_script:
    - pnpm i
  script:
    - npx @hacksawstudios/gitlab-ci-releaser --npm
  only:
    refs:
      - master
  except:
    variables:
      - $CI_COMMIT_MESSAGE =~ /^\d+\.\d+\.\d+/ 

package_rc:
  image: hacksawgaming/hacksaw-build:4.0.0-rc.2-10
  stage: release
  before_script:
    - pnpm i
  script:
    - npx @hacksawstudios/gitlab-ci-releaser --npm --preid rc
  only:
    - develop
  except:
    variables:
      - $CI_COMMIT_MESSAGE =~ /^\d+\.\d+\.\d+(?:-rc\.[0-9]+)?/
